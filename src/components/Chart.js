import React from "react";
import { Card, CardContent, Typography } from "@mui/material";

const Chart = () => (
  <Card>
    <CardContent>
      <Typography variant="h5">Chart</Typography>
      {/* Insert chart code or library here */}
    </CardContent>
  </Card>
);

export default Chart;
